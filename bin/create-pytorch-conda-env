#!/bin/bash
set -e

if [ "$#" -ne "1" ]; then
    echo "usage: $0 {name of new env}"
fi

SCRIPTPATH="$( cd "$(dirname "$0")" ; pwd -P )"

conda env create --name $1 -f $SCRIPTPATH/../pytorch-dev.yaml

cp $SCRIPTPATH/../torch-common.sh ~/.conda/envs/$1/etc/conda/activate.d/
